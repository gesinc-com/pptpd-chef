# Secrets for authentication using CHAP
# client	server	secret	IP addresses
<% node[:pptpd][:chap_secrets].each do |chap_secret| %>
"<%= chap_secret['client'] %>"	"pptpd"	"<%= chap_secret['secret'] %>"	<%= chap_secret['address'] || '*' %>
<% end %>
